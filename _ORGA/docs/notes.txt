func switch_to_level_b():
    level_a.visible = false
    level_a.process_mode = PROCESS_MODE_DISABLED
    
    level_b.visible = true
    level_b.process_mode = PROCESS_MODE_INHERIT
    
    # Teleport player to Level B's workspace
    player.global_position = Vector2(10000, 0)
    
    # Correct the parallax math for Level B's background layers
    # (Assuming you put your Parallax2D nodes in a Node2D container called "Background")
    for parallax_layer in $Level_B/Background.get_children():
        if parallax_layer is Parallax2D:
            parallax_layer.screen_offset.x = 10000
            parallax_layer.scroll_offset.x = 10000